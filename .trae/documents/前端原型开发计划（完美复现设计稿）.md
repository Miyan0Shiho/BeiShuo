## 总体目标
- 基于“细化产品需求与设计文档”完整复现前端原型，使用现有静态架构与本地化资源，像素级对齐设计规范。
- 前端沿用当前原型技术栈：Vue 3 UMD、Tailwind CSS（本地化）、Font Awesome、Vditor、ECharts、iframe 页面组织。

## 现有基础（已核查）
- 入口与预览：`文件资源/碑说 (1)/index.html`，通过 iframe 加载子页面并支持尺寸预览与快捷键（index.html:321-435、assets/javascript/index.js:240-506）。
- 页面树与路由：`assets/javascript/index.js` 中 `tree-node` 组件与 `treeArr` 映射（assets/javascript/index.js:5-75、assets/javascript/initData.js:212-216）。
- 页面清单：`assets/page/*.html` + `treeArr` 的 `url` 字段（Glob 显示 6 个页面含首页与历史记录）。
- 预览尺寸与缩放：`changeResolution`（assets/javascript/index.js:430-445）、`initData.js` 断点集合（assets/javascript/initData.js:1-211）。
- 需求弹窗：Vditor 文档模态（index.html:410-433、assets/javascript/docModal.js）。

## 技术栈与规范
- 保持 UMD 引入模式，不新增构建工具；所有页面以静态 HTML/CSS/JS 交付。
- 样式优先：Tailwind + 自定义 CSS，遵循文档的颜色、字体、间距与响应式断点（需求文档4.2/4.3）。
- 资产复用：本地 Font Awesome、SVG 素材与现有图片资源。
- 交互方式：iframe + postMessage（需求文档4.3），保留键盘导航与预览能力。

## 实施里程碑
1. 基础设施对齐
- 校准资源加载、统一样式入口、规范页面骨架（头部、侧栏、内容区、底部信息）。
- 验收：各子页面在预览窗口内布局稳定、缩放与断点匹配。

2. 首页完美复现
- 按文档主导航结构与核心工作区搭建首页布局与组件（上传入口、功能导航、预览区）。
- 验收：像素对齐主导航、内容区模块化、桌面/移动两端一致性。

3. 碑文识别页
- 交互原型：拖拽/点击上传、进度反馈、错误处理占位、图像预处理提示。
- 验收：流程完整（上传→占位识别结果展示），UI符合规范，移动端操作友好。

4. 校对页
- 左图右文、同步滚动与高亮，单字放大与候选字位占位，版本对比入口。
- 验收：核心交互路径（定位、编辑、提示）可演示，UI对齐。

5. AI阐释页
- 对话式界面原型（消息列表、输入区、流式占位），Markdown 渲染与模板提问。
- 验收：多轮对话原型流程正确、内容区渲染稳定、样式一致。

6. 历史记录页
- 时间线/卡片列表、筛选与搜索占位、收藏与置顶入口。
- 验收：列表密度与层次清晰，筛选区与内容布局协同。

7. 响应式与动效收尾
- 按需求文档断点全面校验，补齐动效与反馈（加载、空态、错误）。
- 验收：所有页面在常见设备断点表现正确；动效统一。

## 页面映射与交付物
- 页面映射来源：`treeArr`（assets/javascript/initData.js:212-216）与 `assets/page/*.html`（Glob 列表）。
- 每个页面交付：
- 静态 HTML 文件（命名沿用现有 uuid），CSS/JS 贴近现有结构。
- 资源引用与组件化片段，遵循 `index.html` 的 iframe 加载逻辑（assets/javascript/index.js:333-346、347-385）。

## 像素级复现策略
- 颜色/字体/间距：提取需求文档规范为 CSS 变量，全局复用。
- 组件网格：采用 Tailwind 工具类严控栅格与间距；必要处用自定义类精调。
- 断点对齐：以文档断点为准（需求文档4.2），逐页校验。
- 资源一致：图标/图片/SVG 与设计稿一致；占位图在未集成后端时以静态资源替代。

## 验证与预览
- 使用现有预览面板与键盘导航验证页面（assets/javascript/index.js:456-474）。
- 校验维度：像素对齐、响应式、交互路径可演示、资源加载性能。

## 第一阶段执行（获得批准后立即开始）
- 调整与统一基础样式入口、搭建首页结构并完成像素级对齐。
- 更新 `treeArr` 对应首页页面，确保预览选择指向最新文件。
- 交付截图与对比说明，并在预览中演示桌面/移动断点。

## 后续说明
- 所有改动在 `feature/techTest` 分支进行，按里程碑逐次提交。
- 不引入新依赖与构建工具，最大限度复用现有资源与结构。