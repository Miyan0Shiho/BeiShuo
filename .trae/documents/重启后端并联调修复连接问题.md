**目标**

* 解决前端请求 `http://localhost:8080/api/v1/ai/chat` 连接被拒绝的问题，重启后端并实时监控日志，确保联调畅通。

**执行步骤**

* 重启后端服务（端口 8080，热重载），注入通义密钥 `LLM_API_KEY`。

* 打开后端预览链接，确认服务存活。

* 你在前端页面再次发起“AI阐释”提问；我持续监控后端输出：

  * 若成功：返回 200 并显示回答与引用来源

  * 若失败：即时定位并修复（鉴权、CORS、密钥、语法结构等）

**说明**

* 连接被拒绝通常是后端进程未运行或端口不可达；本方案将保持后端常驻并热重载修复，确保联调稳定。

