**目标**

* 启动 FastAPI 后端（端口 8080）并持续监控日志，实时提示你测试过程中可能出现的问题与修复建议。

**监控要点**

* 接口状态：`POST /api/v1/ai/chat`、`/ai/chat/stream` 返回码是否为 200

* 鉴权：是否出现 `401 Unauthorized`（令牌问题）

* LLM 调用：是否存在 `Illegal header value b'Bearer '`（API Key 未注入）

* 外部依赖：`All connection attempts failed`（Redis/外部服务未启动，非致命）

* 框架/序列化：是否出现 Pydantic/响应序列化异常

**执行步骤**

* 设置后端环境变量（`LLM_API_KEY`）并启动 `uvicorn`（热重载）

* 持续拉取后端日志，过滤关键 ERROR/WARNING/接口命中信息，并在你执行测试时同步给出诊断与修复建议

**你需要做的**

* 在前端页面“识别 → AI阐释”输入问题并发送；我将边监控后端日志边反馈状态与解决方案。

